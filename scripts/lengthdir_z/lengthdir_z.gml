/// @function lengthdir_z(len, dir)
/// @description Calculates the z-component of a vector in 3D space using degrees.
/// @param {real} len The length of the vector.
/// @param {real} dir The direction (pitch) in degrees.
function lengthdir_z(len, dir) {
    return lengthdir_y(len, dir);
}